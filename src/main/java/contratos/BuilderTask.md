Project context:

- Java Maven project
- Scenario 1 already implemented using Prototype pattern in package "inventory"
- Do NOT modify inventory package
- We are implementing Scenario 2 using Builder pattern.

Goal:
Create a rental contract builder system.

Requirements:
- Contract requires: Cliente, Vehiculo, plan, duracionDias
- Optional: GPS, seguro, cargadorPortatil
- If duration > 30 days apply discount automatically
- Use Builder Pattern correctly
- Code must compile with existing classes